// const express = require("express");

// const App = express();

// App.get("/", (req, res) => {
//   res.send("Hello");
// });

// App.get("/about", (req, res) => {
//   res.send("Acerca de ?");
// });

// App.get("/clima", (req, res) => {
//   res.send("El clima es nice");
// });

// // Si las rutas que trata de ingresar el usuario no existen recibira esta respuesta
// App.use((req, res) => {
//   res.status(404).send("no se encontro tu pagina");
// });

// const puerto = 3000;

// App.listen(puerto);
// console.log(`Servidor escuchando en el puerto....... ${puerto}`);
